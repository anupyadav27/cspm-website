# social-posts — publishing folder (finals only)

One folder per released package: **final platform media + copy.md, nothing else**.
All production (framework, pipeline, working files, cuts, audits) lives in the isolated
studio workspace: `~/Desktop/onam-studio/` — see its README for the produce → audit →
release → publish flow.

To publish: open the package folder, paste `copy.md` sections into each platform,
attach the matching media file per the table at the top of copy.md.
