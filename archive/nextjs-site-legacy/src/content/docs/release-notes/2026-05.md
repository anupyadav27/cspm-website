# Release Notes — May 2026

> **Release cadence:** Monthly. Published on the first business day of each month.
> **Changelog format:** **New** · **Changed** · **Fixed** · **Deprecated** · **Breaking**
> **Status page:** [status.onam.io](https://status.onam.io) for real-time deployment progress.

---

## Platform Highlights — May 2026

This month's release focused on three themes: **discovery catalog completeness** (165 cloud resources backfilled), **investigation journey UX** for inventory and the threat center, and the **SecOps engine** (code-repo onboarding, account-ID propagation, security blocker remediation).

| Highlight | What changed | Where it shows up |
|---|---|---|
| **165 cloud resources backfilled to catalog** | IBM Cloud (99 types) and Kubernetes (66 types) now have complete catalog entries — every discovery emits a normalized item record | Inventory → Asset list now shows full type coverage for IBM and K8s |
| **Investigation journeys** | Asset-centric journey for Inventory · attack-path-first journey for Threat Center · 5 new tabs per asset | Click any asset or attack path to see the new side-panel investigation view |
| **SecOps engine** | VCS provider tiles for GitHub / GitLab / Bitbucket / Azure Repos · `account_id` propagation across SAST/DAST/SCA findings · Q1 audit blockers cleared | Onboarding → Code Repositories now shows VCS tiles with status |
| **Technology engine** | Linux technology support added — package managers, installed software inventory, OS config, kernel parameters · ~150 new tech rules | Tech Discovery / Tech Check views now include Linux workloads |
| **Billing engine** | Commercial subscription management, payment integration, audit trail | Settings → Billing now shows full subscription lifecycle |

---

## Discovery Engine

### New
- **IBM Cloud catalog backfill** — 99 IBM cloud resources that previously lacked a canonical catalog entry now emit a complete record. Affected resource types include VPC, VSI, Cloud Object Storage, IAM, Key Protect, Activity Tracker, and IBM Kubernetes Service workloads.
- **Kubernetes catalog backfill** — 66 Kubernetes resource types (ClusterRoles, NetworkPolicies, PersistentVolumes, Ingress, HPA, PodDisruptionBudgets, and others) now emit complete catalog items.

### Changed
- Catalog-completeness check added to internal release validation — fails the release if any new discovery type is missing a catalog entry. Prevents the regression class entirely.
- 5 nested-envelope bugs fixed where discovery payloads were double-wrapped — affected payload structure for some Kubernetes and IBM resource types.

---

## Inventory Engine

### New
- **Asset investigation journey** — clicking any asset from the inventory table now opens a side panel with **5 tabs**: Overview, Findings, Relationships, CIEM, and History. Replaces the previous flat detail view.

### Changed
- **CIEM tab added** to the asset side panel — shows effective permissions and unused entitlements for identity resources (users, roles, service accounts).

---

## Threat Engine

### New
- **Threat center investigation journey** — attack-path-first view. Clicking an attack path opens a full-screen investigation panel with: Attack Path graph, Techniques tab (MITRE ATT&CK detail), Affected Resources tab, and Remediation tab.
- **Node investigation panel** — click any node in the security graph to open a contextual investigation panel showing the node's findings, connected nodes, and blast radius.
- **Technique detail modal** — drill into any MITRE ATT&CK technique for description, detection logic, and remediation guidance.

### Fixed
- **Security graph build endpoint** — resolved a high-priority issue in graph build that could return findings from other tenants under specific timing conditions. Tenant isolation tests added to the regression suite.

---

## SecOps Engine

### New
- **VCS provider tiles** — onboarding UI now shows GitHub, GitLab, Bitbucket, and Azure Repos as visual tiles with connection status.
- **Account ID flow** — `account_id` is now correctly propagated through all SAST / DAST / SCA finding records, enabling cross-domain correlation with discovery and check findings.

### Fixed
- Security blockers from Q1 audit resolved — input validation on repository URLs, SSRF prevention on webhook callbacks, tenant isolation on shared scanner workloads.

---

## Technology Engine

### New
- **Linux technology support** — technology discovery and check now scan Linux-based workloads. Covers package managers (apt, yum, rpm), installed software inventory, OS configuration, and kernel parameters. Adds **~150 new technology rules**.

---

## Billing Engine

### New
- **Commercial subscription management** — full subscription lifecycle (create, upgrade, downgrade, cancel, refund) with audit trail.
- **Payment integration** — secure tokenized payment processing with PCI DSS-compliant handling.
- **Audit trail** — every billing action is recorded in the audit log per SOC 2 requirements; available for export.

---

## API & Integrations

### Fixed
- **Encryption enrichment loops resolved** — dashboard views no longer time out on tenants with large numbers of KMS keys.
- **Security group fallback corrected** — network topology view now returns correct effective exposure when security-group rules reference other security groups (not just CIDR blocks).

---

## Platform (Identity & Auth)

### Fixed
- **Platform admin default tenant** — `platform_admin` users with no explicit tenant assignment no longer receive a 500 error on first login.

---

## Container Security

### Fixed
- **Permission key corrected** — `container_security:read` → `container:read`. RBAC gate was blocking `analyst` role from accessing container findings.

---

## CWPP

### Fixed
- **Null-handling in serverless and runtime aggregation** — `rule_id` and `title` fields can be `null` for certain resource types. Engine now handles null values cleanly during aggregation.

---

## Breaking Changes

None this month.

---

## Deprecations

None this month.

---

## Upgrade Notes

All engines deploy independently. **Customers don't take any upgrade action** — the platform handles deployments transparently during the published maintenance window. The recommended internal deploy order (for transparency) is:

| Order | Engine | Notes |
|---|---|---|
| 1 | Discovery | Catalog backfill — must precede inventory |
| 2 | Inventory | Asset investigation journey · consumes new catalog items |
| 3 | Check | No changes — included for ordering hygiene |
| 4 | Threat | Investigation journey + tenant-isolation fix |
| 5 | All domain engines | Order-independent after Threat |
| 6 | API Gateway / BFF | Consumes new domain engine outputs |

If you're on a self-hosted Enterprise plan, your account manager will coordinate the deploy window with your team.

---

## Upcoming in June 2026

| Feature | Surface | Status |
|---|---|---|
| Auth & registration wizard | Platform | Planned |
| Onboarding revamp — tenant-first flow | Onboarding | Design |
| Security graph — CVE nodes integration | Threat | In development |
| Config-based discovery (cloud-native event-driven scanning) | Discovery | Design |
| Onam CLI scanner | SecOps | Planned |
| Trust Center automation | Platform | In development |

---

## Previous Releases

- April 2026 _(coming soon)_
- March 2026 _(coming soon)_

---

*Published: 2026-05-09*
*For questions about a specific change: support@onam.io*
*To report a regression: open a support ticket from your account dashboard*
