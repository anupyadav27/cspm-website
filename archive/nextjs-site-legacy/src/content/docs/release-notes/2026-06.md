---
title: Release Notes — June 2026
description: Major platform expansion — Attack Path Analysis, CDR, Risk Quantification, Code Security (SecOps), Technology Engine v2, and a unified security findings layer.
---

# Release Notes — June 2026

This is a major platform release. June 2026 brings four new security engines, a unified findings architecture, and a complete overhaul of the posture snapshot and data pipeline. These changes reflect the consolidation from 11 to 16+ active security engines.

---

## New Engines

### Attack Path Analysis
**Available: June 2026**

The Attack Path engine replaces the retired threat-v1 engine with a ground-up rewrite purpose-built for graph-based security analysis.

**What's new:**
- Security graph built from all posture, identity, network, and vulnerability findings
- Crown jewel path analysis — every route from exposed entry points to tagged critical assets
- Toxic combination detection — automatically surfaces pairs/triples of Medium findings that together create Critical blast radius
- MITRE ATT&CK technique tagging on every graph node and edge
- Attack path visualizer (interactive graph in the Onam platform UI)
- Integration with the Risk engine — every attack path terminus carries FAIR dollar exposure estimate

**Migration note:** The retired `engine-threat` and `engine-threat-v1` deployments have been removed. Attack path findings are now in the `attack_paths` table in the `threat_engine_attack_path` database.

---

### CDR — Cloud Detection & Response
**Available: June 2026**

Three-tier behavioral threat detection running as an independent continuous pipeline (not part of the main scan DAG).

**What's new:**
- L1 rule-based detection: 200+ signatures across CloudTrail, VPC Flow, and K8s audit logs
- L2 statistical behavioral baselines: per-entity anomaly detection over 30-day rolling windows
- L3 ML-based detection: unsupervised anomaly detection for novel threats
- Full MITRE ATT&CK for Cloud mapping on all detections
- Automatic incident correlation across detection tiers
- Slack, PagerDuty, and email alerting with full context
- Response playbooks linked from every incident

---

### Risk Quantification (FAIR Model)
**Available: June 2026**

The Risk engine now runs the full FAIR pipeline on every finding, outputting dollar-denominated financial exposure estimates.

**What's new:**
- FAIR model scoring: P50 and P90 loss estimates for every open finding
- Regulatory fine projection: GDPR, HIPAA, PCI-DSS, and SOX exposure per finding
- Crown jewel risk multipliers (2×–5× based on asset criticality)
- Blast radius quantification: assets, users, and data records at risk
- Risk reduction ranking: remediation queue ordered by dollar exposure eliminated
- Executive dashboard: total exposure, top-10 risks, 90-day trend
- Full integration with Attack Path engine

---

### Code Security (SecOps)
**Available: June 2026**

Unified code security platform with SAST, DAST, SCA, and IaC scanning.

**What's new:**
- SAST: 2,852 rules across 14 languages (Python, Go, Java, JS, TS, Ruby, PHP, C/C++, C#, Rust, Kotlin, Swift, Terraform, HCL)
- DAST: 479 active test payloads across 12 vulnerability classes
- SCA: full dependency graph with CVE + EPSS + KEV enrichment
- IaC: Terraform, CloudFormation, Helm, K8s manifests, ARM templates, Bicep, Pulumi
- CycloneDX SBOM generation for all repositories and services
- AI-powered fix suggestions with cloud context awareness (Mistral-based)
- CI/CD blocking gates for GitHub Actions, GitLab CI, Jenkins, CircleCI
- Findings correlated with cloud posture for context-aware severity boosting

---

## Technology Engine v2

**Upgraded: June 2026**

The Technology Engine (port 8030–8033) has been significantly expanded.

**What's new:**
- **5,025 detection rules** (up from ~2,000) across 34 technologies
- 4 sub-engines now running independently: tech-discovery, tech-inventory, tech-check, tech-ciem
- CIEM behavioral analysis integrated into tech engine (tech-ciem sub-engine)
- Runtime technology inventory correlated with CVE data for version-aware vulnerability detection
- 34 supported technologies including: Java, Python, Node.js, Go, Ruby, PHP, .NET, Nginx, Apache, Redis, PostgreSQL, MySQL, MongoDB, Elasticsearch, Kafka, RabbitMQ, and more

---

## Unified Security Findings Layer

**Shipped: June 2026**

All 16+ engines now write to a single `security_findings` table in the DI database. This replaces the previous per-engine report tables (all now retired).

**Impact:**
- Unified findings view across all engines in a single query
- Cross-engine finding correlation (e.g., a CVE finding + an IAM finding on the same resource)
- Consistent `last_seen_at` tracking across all finding types
- BFF layer reads exclusively from `security_findings` — no engine-specific report tables

**Retired tables (all dropped June 2026):**
`iam_report`, `network_report`, `datasec_report`, `compliance_report`, `cdr_report`, `container_sec_report`, `dbsec_report`, `encryption_report`, `ai_security_report`, `risk_report`, `risk_summary`, `risk_trends`, `risk_input_transformed`

---

## DI Engine — Discovery & Inventory Consolidation

**Shipped: May 2026 (documented June 2026)**

The DI engine is now the single source of truth for all asset inventory, replacing the retired discoveries and inventory engines.

- `asset_inventory_*` tables replace all retired discovery/inventory tables
- `asset_relationships` stores all cross-engine relationship data
- `posture_snapshot` replaces all `*_report` tables for engine status
- `resource_security_posture` stores per-resource posture scores

**Retired engines:** `engine-discoveries`, `engine-inventory`, `engine-threat`, `engine-threat-v1` — all K8s deployments removed and databases dropped.

---

## Scope Column Standardization

**Shipped: June 2026**

All findings tables now carry 6 mandatory scope columns:

| Column | Description |
|---|---|
| `tenant_id` | NOT NULL — primary isolation key |
| `customer_id` | Customer within tenant |
| `account_id` | Cloud account ID |
| `provider` | aws / azure / gcp / oci / alibaba / ibm / k8s |
| `region` | Cloud region (DEFAULT 'global' for global services) |
| `scan_run_id` | Argo DAG run ID for traceability |

Findings tables without all 6 columns are rejected at the quality gate.

---

## Rule Count

Total active detection rules across all engines as of June 2026:

| Engine | Rules |
|---|---|
| CSPM (Check engine) | 1,918 |
| Technology Engine | 5,025 |
| Code Security (SecOps) | 2,852 |
| DAST payloads | 479 |
| IAM Security | 57 |
| Data Security | 62 |
| CDR detection signatures | 200+ |
| Other engine rules | 500+ |
| **Total** | **~10,000+** |

---

## Bug Fixes

- **Attack path**: Fixed duplicate node naming in attack path graph causing display errors in the UI path visualizer
- **Compliance**: Fixed stuck scans caused by rule_control_mapping data gap — 565 previously invisible failing controls now visible
- **AI Security**: Fixed Bedrock API error handling (4-bug chain); 510 AI findings now flowing to security_findings
- **Network Security**: Fixed INSERT-only path for network_findings (now upserts with last_seen_at tracking)
- **Narrative engine**: Fixed infinite scan poll loop; fixed discoveries→DI DB migration; silenced rule_control_mapping noise

---

*Previous release notes: [May 2026](/docs/release-notes/2026-05)*
